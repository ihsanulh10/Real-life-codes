#include<stdio.h>
#include<math.h>
int main()
{
	int x,y,i,j,ans;
	printf("Enter the positon of x and y\n");
	scanf("%d%d",&x,&y);
	for(i=1;i<=8;i++)
	{
	for(j=1;j<=8;j++)
	{
		ans=pow((x-i),2)+pow((y-j),2);
	if(ans==5)
	{
		printf("the position of the knight is =%d %d\n",i,j);
	}
}
}	
return 0;
}
